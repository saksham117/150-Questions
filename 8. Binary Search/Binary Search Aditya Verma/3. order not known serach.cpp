// we are given that array is sorted, but not told, whether in the ascending or the descending order.

// so its simple
// take the zero th and 1st element
// if zero the element is smaller than first, then apply normal binary serach

// else apply reverse binary search.

// the size of the array is infinite

// start = 0
// high = n (amy n you want)

// if arr[high] < key

// start = high + 1
// high = 2*high

// keep on doing this till element comes within start and high
// then apply binary search
